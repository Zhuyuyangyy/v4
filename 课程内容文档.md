# EduMind 课程内容文档

> 基于项目代码结构提取并重新梳理的完整课程体系，涵盖 **5 大方向、24 门课程**，包含知识点、代码示例、流程图、思维导图、学习场景及预设问答等数据。

---

## 目录

- [一、编程与算法基础（7门）](#一编程与算法基础7门)
- [二、计算机系统（4门）](#二计算机系统4门)
- [三、软件工程（2门）](#三软件工程2门)
- [四、人工智能方向（8门）](#四人工智能方向8门)
- [五、前沿与应用（3门）](#五前沿与应用3门)
- [学习场景与辅导模式](#学习场景与辅导模式)
- [代码示例汇总](#代码示例汇总)
- [学习资源](#学习资源)
- [学习路径](#学习路径)

---

## 一、编程与算法基础（7门）

### 1. C语言程序设计

- **难度**: 入门
- **前置知识**: 无
- **主题色**: `#00599C`
- **图标**: Code

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 基本语法 | 数据类型、运算符、表达式、输入输出 | 入门 |
| 流程控制 | if-else、switch、for、while、do-while | 入门 |
| 函数 | 函数定义与调用、参数传递、递归 | 入门 |
| 数组与字符串 | 一维/二维数组、字符数组与字符串处理 | 入门 |
| 指针 | 指针概念、指针运算、指针与数组、函数指针 | 进阶 |
| 结构体与共用体 | 结构体定义、嵌套、联合体、枚举 | 进阶 |
| 动态内存管理 | malloc、calloc、realloc、free | 进阶 |
| 文件操作 | 文件打开/关闭、读写操作、二进制文件 | 进阶 |
| 预处理 | 宏定义、文件包含、条件编译 | 入门 |

#### 代码示例

```c
#include <stdio.h>

// 递归实现斐波那契数列
int fibonacci(int n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// 指针交换
void swap(int *a, int *b) {
    int temp = *a;
    *a = *b;
    *b = temp;
}

int main() {
    int n = 10;
    printf("Fibonacci(%d) = %d\n", n, fibonacci(n));
    
    int x = 5, y = 10;
    swap(&x, &y);
    printf("x = %d, y = %d\n", x, y);
    return 0;
}
```

#### 预设问答
- 指针和数组有什么区别和联系？
- 什么是野指针？如何避免？
- malloc 和 calloc 有什么区别？
-  static 关键字在 C 语言中有哪些作用？

---

### 2. Python程序设计

- **难度**: 入门
- **前置知识**: 无
- **主题色**: `#3776AB`
- **图标**: Code

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 基础语法 | 变量、数据类型、运算符、输入输出 | 入门 |
| 流程控制 | if-elif-else、for、while、break、continue | 入门 |
| 数据结构 | 列表、元组、字典、集合、推导式 | 入门 |
| 函数 | 函数定义、参数类型、lambda、装饰器 | 入门 |
| 文件与异常 | 文件读写、with 语句、异常处理 | 入门 |
| 面向对象 | 类与对象、继承、多态、特殊方法 | 进阶 |
| 模块与包 | 模块导入、包管理、pip 使用 | 入门 |
| 高级特性 | 生成器、迭代器、上下文管理器、闭包 | 进阶 |

#### 代码示例

```python
# 装饰器示例
def timer(func):
    import time
    def wrapper(*args, **kwargs):
        start = time.time()
        result = func(*args, **kwargs)
        print(f"{func.__name__} 耗时: {time.time() - start:.4f}s")
        return result
    return wrapper

@timer
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

# 生成器示例
def fibonacci_gen(n):
    a, b = 0, 1
    for _ in range(n):
        yield a
        a, b = b, a + b

print(list(fibonacci_gen(10)))
```

#### 预设问答
- Python 中列表和元组的区别是什么？
- 什么是列表推导式？有什么优点？
- 生成器和迭代器有什么区别？
- Python 的 GIL 是什么？对多线程有什么影响？

---

### 3. 面向对象程序设计（Java）

- **难度**: 进阶
- **前置知识**: 程序设计基础
- **主题色**: `#ED8B00`
- **图标**: Code

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 类与对象 | 类的定义、对象创建、构造方法、this 关键字 | 入门 |
| 封装 | 访问修饰符、getter/setter、包机制 | 入门 |
| 继承 | extends、super、方法重写、final 关键字 | 进阶 |
| 多态 | 方法重载、动态绑定、抽象类、接口 | 进阶 |
| 异常处理 | try-catch-finally、自定义异常、throws | 入门 |
| 泛型 | 泛型类、泛型方法、类型通配符 | 进阶 |
| 集合框架 | List、Set、Map、Collections 工具类 | 进阶 |
| 多线程 | Thread、Runnable、synchronized、线程池 | 高级 |
| I/O 流 | 字节流、字符流、缓冲流、对象序列化 | 进阶 |
| Lambda 与 Stream | 函数式接口、Stream API、Optional | 高级 |

#### 代码示例

```java
import java.util.*;
import java.util.stream.*;

// 泛型类和 Stream API 示例
public class CourseManager<T extends Course> {
    private List<T> courses = new ArrayList<>();
    
    public void add(T course) {
        courses.add(course);
    }
    
    public List<T> filterByDifficulty(String difficulty) {
        return courses.stream()
            .filter(c -> c.getDifficulty().equals(difficulty))
            .collect(Collectors.toList());
    }
    
    // 多线程示例
    public void processInParallel() {
        courses.parallelStream()
            .forEach(c -> System.out.println(
                Thread.currentThread().getName() + ": " + c.getName()
            ));
    }
}

abstract class Course {
    private String name;
    private String difficulty;
    
    public Course(String name, String difficulty) {
        this.name = name;
        this.difficulty = difficulty;
    }
    
    public abstract void start();
    
    public String getName() { return name; }
    public String getDifficulty() { return difficulty; }
}
```

#### 预设问答
- Java 中 == 和 equals() 有什么区别？
- 接口和抽象类有什么区别？如何选择？
- HashMap 的底层实现原理是什么？
- synchronized 和 volatile 关键字的区别？

---

### 4. 面向对象程序设计（C++）

- **难度**: 进阶
- **前置知识**: C 语言基础
- **主题色**: `#00599C`
- **图标**: Code

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| C++ 增强 | 引用、函数重载、默认参数、命名空间 | 入门 |
| 类与对象 | 类的定义、构造函数/析构函数、拷贝构造 | 入门 |
| 继承与派生 | 继承方式、多继承、虚继承、菱形继承 | 进阶 |
| 多态 | 虚函数、纯虚函数、抽象类、虚函数表 | 进阶 |
| 运算符重载 | 二元/一元运算符重载、类型转换 | 进阶 |
| 模板 | 函数模板、类模板、模板特化 | 进阶 |
| STL 容器 | vector、list、map、set、unordered_map | 进阶 |
| 智能指针 | unique_ptr、shared_ptr、weak_ptr | 高级 |
| 移动语义 | 右值引用、move 语义、完美转发 | 高级 |
| 异常处理 | try-catch、RAII、noexcept | 进阶 |

#### 代码示例

```cpp
#include <iostream>
#include <vector>
#include <memory>
#include <algorithm>

// 模板 + 智能指针 + STL 综合示例
template<typename T>
class DataManager {
private:
    std::vector<std::shared_ptr<T>> data;
public:
    void add(std::shared_ptr<T> item) {
        data.push_back(std::move(item));
    }
    
    void sort() {
        std::sort(data.begin(), data.end(),
            [](const auto& a, const auto& b) { return *a < *b; });
    }
    
    void display() const {
        for (const auto& item : data) {
            std::cout << *item << " ";
        }
        std::cout << std::endl;
    }
};

// RAII 示例
class FileHandler {
private:
    FILE* file;
public:
    FileHandler(const char* filename) {
        file = fopen(filename, "r");
        if (!file) throw std::runtime_error("Cannot open file");
    }
    ~FileHandler() { if (file) fclose(file); }
    // 禁止拷贝
    FileHandler(const FileHandler&) = delete;
    FileHandler& operator=(const FileHandler&) = delete;
};
```

#### 预设问答
- C++ 中虚函数是如何实现的？虚函数表是什么？
- 智能指针的原理是什么？为什么要用智能指针？
- 什么是 RAII？在 C++ 中如何应用？
- 左值引用和右值引用的区别是什么？

---

### 5. 数据结构

- **难度**: 进阶
- **前置知识**: C/C++ 或 Java 程序设计
- **主题色**: `#4CAF50`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 线性表 | 顺序表、链表（单链表、双链表、循环链表） | 入门 |
| 栈与队列 | 顺序栈、链栈、循环队列、双端队列 | 入门 |
| 字符串 | KMP 算法、BM 算法、模式匹配 | 进阶 |
| 树与二叉树 | 二叉树遍历、线索二叉树、哈夫曼树、BST | 进阶 |
| 堆 | 最大堆、最小堆、堆排序、优先队列 | 进阶 |
| 图 | 邻接矩阵/表、DFS、BFS、最短路径、最小生成树 | 高级 |
| 散列表 | 哈希函数、冲突解决（链地址/开放地址） | 进阶 |
| 平衡树 | AVL 树、红黑树、B 树、B+ 树 | 高级 |
| 并查集 | 路径压缩、按秩合并 | 进阶 |
| 高级数据结构 | 线段树、树状数组、Trie 树、跳表 | 高级 |

#### 代码示例（KMP 字符串匹配）

```python
def build_lps(pattern):
    """构建最长公共前后缀数组"""
    lps = [0] * len(pattern)
    length = 0
    i = 1
    while i < len(pattern):
        if pattern[i] == pattern[length]:
            length += 1
            lps[i] = length
            i += 1
        else:
            if length != 0:
                length = lps[length - 1]
            else:
                lps[i] = 0
                i += 1
    return lps

def kmp_search(text, pattern):
    """KMP 字符串匹配"""
    lps = build_lps(pattern)
    i = j = 0
    positions = []
    while i < len(text):
        if text[i] == pattern[j]:
            i += 1; j += 1
            if j == len(pattern):
                positions.append(i - j)
                j = lps[j - 1]
        else:
            if j != 0:
                j = lps[j - 1]
            else:
                i += 1
    return positions
```

#### 预设问答
- 链表和数组各自的优缺点是什么？
- 什么是时间复杂度和空间复杂度？如何分析？
- 哈希表如何解决冲突？链地址法和开放地址法对比
- B+ 树相比 B 树有哪些优势？为什么数据库常用 B+ 树？

---

### 6. 算法设计与分析

- **难度**: 高级
- **前置知识**: 数据结构、程序设计
- **主题色**: `#FF5722`
- **图标**: Target

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 算法复杂度 | 大 O 表示法、渐进分析、摊还分析 | 进阶 |
| 递归与分治 | 二分搜索、归并排序、快速排序、大整数乘法 | 进阶 |
| 动态规划 | 背包问题、LCS、最短路径、矩阵连乘 | 高级 |
| 贪心算法 | 活动选择、哈夫曼编码、最小生成树 | 进阶 |
| 回溯法 | 八皇后、数独、子集和、图着色 | 高级 |
| 分支限界 | 旅行商问题、0-1 背包、任务分配 | 高级 |
| 图算法 | 拓扑排序、强连通分量、最大流 | 高级 |
| NP 完全理论 | P 与 NP、NP 完全、归约证明 | 高级 |
| 近似算法 | 顶点覆盖、旅行商近似 | 高级 |
| 随机化算法 | 蒙特卡洛、拉斯维加斯、随机快速排序 | 高级 |

#### 代码示例（动态规划 — 0-1 背包）

```python
def knapsack_01(weights, values, capacity):
    """0-1 背包问题动态规划解法"""
    n = len(weights)
    dp = [[0] * (capacity + 1) for _ in range(n + 1)]
    
    for i in range(1, n + 1):
        for w in range(1, capacity + 1):
            if weights[i-1] <= w:
                dp[i][w] = max(
                    values[i-1] + dp[i-1][w - weights[i-1]],
                    dp[i-1][w]
                )
            else:
                dp[i][w] = dp[i-1][w]
    
    # 回溯找出选择
    selected = []
    w = capacity
    for i in range(n, 0, -1):
        if dp[i][w] != dp[i-1][w]:
            selected.append(i-1)
            w -= weights[i-1]
    
    return dp[n][capacity], selected

# 示例
weights = [2, 3, 4, 5]
values = [3, 4, 5, 6]
capacity = 8
max_val, items = knapsack_01(weights, values, capacity)
print(f"最大价值: {max_val}, 选中物品: {items}")
```

#### 预设问答
- 动态规划和分治算法的区别和联系是什么？
- 什么情况下适合使用贪心算法？
- P 类和 NP 类问题的本质区别是什么？
- 如何证明一个问题是 NP 完全的？

---

### 7. 编译原理

- **难度**: 高级
- **前置知识**: 数据结构、程序设计语言
- **主题色**: `#9C27B0`
- **图标**: GitCompare

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 编译过程概述 | 词法分析、语法分析、语义分析、中间代码生成、代码优化、目标代码生成 | 入门 |
| 词法分析 | 正则表达式、有限自动机（NFA/DFA）、词法分析器生成 | 进阶 |
| 语法分析 | 上下文无关文法、LL(1) 分析器、LR 分析器（SLR/LALR/LR(1)） | 高级 |
| 语义分析 | 属性文法、语法制导翻译、类型检查 | 高级 |
| 中间代码 | 三地址码、抽象语法树、DAG 表示 | 进阶 |
| 运行时环境 | 存储组织、栈分配、堆管理、垃圾回收 | 进阶 |
| 代码优化 | 常量折叠、公共子表达式消除、循环优化、数据流分析 | 高级 |
| 目标代码生成 | 指令选择、寄存器分配、指令调度 | 高级 |

#### 代码示例（简易计算器 — 递归下降语法分析）

```python
class SimpleCalculator:
    """递归下降语法分析器 — 支持 + - * / ( )"""
    def __init__(self, expr):
        self.tokens = list(expr.replace(' ', ''))
        self.pos = 0
    
    def parse(self):
        result = self.parse_expr()
        if self.pos < len(self.tokens):
            raise ValueError("Unexpected token")
        return result
    
    def parse_expr(self):
        result = self.parse_term()
        while self.pos < len(self.tokens):
            op = self.tokens[self.pos]
            if op in ('+', '-'):
                self.pos += 1
                right = self.parse_term()
                result = result + right if op == '+' else result - right
            else:
                break
        return result
    
    def parse_term(self):
        result = self.parse_factor()
        while self.pos < len(self.tokens):
            op = self.tokens[self.pos]
            if op in ('*', '/'):
                self.pos += 1
                right = self.parse_factor()
                if op == '*': result *= right
                else: result /= right
            else:
                break
        return result
    
    def parse_factor(self):
        if self.tokens[self.pos] == '(':
            self.pos += 1
            result = self.parse_expr()
            if self.tokens[self.pos] != ')':
                raise ValueError("Missing )")
            self.pos += 1
            return result
        # 解析数字
        num = ''
        while self.pos < len(self.tokens) and self.tokens[self.pos].isdigit():
            num += self.tokens[self.pos]
            self.pos += 1
        return int(num)

calc = SimpleCalculator("3 + 5 * (2 + 4)")
print(calc.parse())  # 33
```

#### 预设问答
- 编译器和解释器的区别是什么？
- LL(1) 和 LR(1) 分析器各自的特点是什么？
- NFA 和 DFA 的关系是什么？如何将 NFA 转换为 DFA？
- 什么是 SSA（静态单赋值）形式？它有什么好处？

---

## 二、计算机系统（4门）

### 8. 计算机组成原理

- **难度**: 进阶
- **前置知识**: 数字逻辑、程序设计基础
- **主题色**: `#607D8B`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 计算机系统概论 | 冯·诺依曼结构、计算机层次结构、性能指标 | 入门 |
| 数据表示 | 原码/反码/补码、浮点数 IEEE754、字符编码 | 进阶 |
| 运算方法与运算器 | 定点加减乘除、浮点运算、ALU 原理 | 进阶 |
| 存储系统 | 存储器层次结构、Cache、虚拟存储器、DRAM 刷新 | 进阶 |
| 指令系统 | 指令格式、寻址方式、CISC 与 RISC | 进阶 |
| 中央处理器 | CPU 数据通路、微程序控制、流水线、冒险处理 | 高级 |
| 总线系统 | 总线结构、总线仲裁、总线标准 | 进阶 |
| 输入输出系统 | I/O 接口、中断机制、DMA、通道处理机 | 进阶 |

#### 预设问答
- 冯·诺依曼结构的核心思想是什么？
- Cache 的写策略有哪些？写直达和写回的区别？
- 什么是流水线冒险？有哪些解决方式？
- CISC 和 RISC 的主要区别是什么？

---

### 9. 操作系统

- **难度**: 进阶
- **前置知识**: 计算机组成原理、程序设计
- **主题色**: `#1565C0`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 操作系统概述 | OS 定义与功能、发展历史、系统调用 | 入门 |
| 进程与线程 | 进程状态、PCB、线程模型、进程通信（IPC） | 进阶 |
| CPU 调度 | 调度算法（FCFS/SJF/RR/优先级/多级队列） | 进阶 |
| 同步与互斥 | 临界区、信号量、管程、生产者消费者、读者写者 | 高级 |
| 死锁 | 死锁条件、死锁预防/避免/检测/解除、银行家算法 | 进阶 |
| 内存管理 | 分页、分段、段页式、TLB、页面置换算法 | 进阶 |
| 文件系统 | 文件结构、目录实现、磁盘调度、空间管理 | 进阶 |
| 设备管理 | 设备独立性、SPOOLing、缓冲技术 | 进阶 |
| 虚拟化 | 虚拟机监视器、容器技术、地址空间虚拟化 | 高级 |

#### 代码示例（生产者消费者 — 信号量实现）

```python
import threading
import time
import random

class BoundedBuffer:
    def __init__(self, capacity=5):
        self.capacity = capacity
        self.buffer = []
        self.mutex = threading.Semaphore(1)
        self.empty = threading.Semaphore(capacity)
        self.full = threading.Semaphore(0)
    
    def produce(self, item):
        self.empty.acquire()
        self.mutex.acquire()
        self.buffer.append(item)
        print(f"生产: {item}, 缓冲区: {self.buffer}")
        self.mutex.release()
        self.full.release()
    
    def consume(self):
        self.full.acquire()
        self.mutex.acquire()
        item = self.buffer.pop(0)
        print(f"消费: {item}, 缓冲区: {self.buffer}")
        self.mutex.release()
        self.empty.release()
        return item
```

#### 预设问答
- 进程和线程有什么区别？
- 什么是死锁？产生死锁的四个必要条件是什么？
- 虚拟内存是如何实现的？页面置换算法有哪些？
- 进程间通信的方式有哪些？

---

### 10. 计算机网络

- **难度**: 进阶
- **前置知识**: 计算机组成原理
- **主题色**: `#0277BD`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 网络体系结构 | OSI 七层模型、TCP/IP 四层模型、协议与服务 | 入门 |
| 物理层 | 信号编码、调制技术、传输介质、复用技术 | 入门 |
| 数据链路层 | 差错检测（CRC）、流量控制、滑动窗口、CSMA/CD | 进阶 |
| 网络层 | IP 协议、子网划分、CIDR、路由算法（RIP/OSPF/BGP） | 进阶 |
| 传输层 | TCP/UDP 协议、TCP 拥塞控制、三次握手与四次挥手 | 进阶 |
| 应用层 | HTTP/HTTPS、DNS、SMTP、FTP、DHCP | 进阶 |
| 网络安全 | 对称/非对称加密、SSL/TLS、防火墙、VPN | 高级 |
| 无线网络 | Wi-Fi、蓝牙、移动通信网络、Ad Hoc 网络 | 进阶 |

#### 代码示例（TCP 套接字编程）

```python
# TCP 服务器
import socket
import threading

def handle_client(client_socket, addr):
    print(f"新连接: {addr}")
    while True:
        data = client_socket.recv(1024)
        if not data:
            break
        client_socket.send(f"Echo: {data.decode()}".encode())
    client_socket.close()

server = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
server.bind(('0.0.0.0', 8888))
server.listen(5)
print("服务器启动于 8888 端口...")

while True:
    client, addr = server.accept()
    threading.Thread(target=handle_client, args=(client, addr)).start()
```

#### 预设问答
- TCP 三次握手和四次挥手的过程是什么？
- TCP 拥塞控制的算法有哪些？
- HTTP 和 HTTPS 的区别是什么？
- 从输入 URL 到页面显示，中间经历了什么？

---

### 11. 数据库系统原理

- **难度**: 进阶
- **前置知识**: 数据结构、程序设计
- **主题色**: `#336791`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 数据库系统概论 | 数据模型、三级模式两级映像、数据库系统组成 | 入门 |
| 关系模型 | 关系代数、元组/域关系演算、关系完整性 | 入门 |
| SQL 语言 | DDL/DML/DCL、嵌套查询、连接查询、视图 | 进阶 |
| 关系数据库设计 | 函数依赖、范式（1NF~BCNF）、模式分解 | 进阶 |
| 索引与存储 | B+ 树索引、哈希索引、聚簇索引、存储引擎 | 进阶 |
| 事务管理 | ACID 特性、并发控制、封锁协议、隔离级别 | 高级 |
| 查询优化 | 查询代价估计、连接算法、查询重写 | 高级 |
| NoSQL 数据库 | 键值数据库、文档数据库、图数据库、列族数据库 | 进阶 |

#### 代码示例

```sql
-- 学生选课系统 SQL 示例
CREATE TABLE Student (
    sno CHAR(10) PRIMARY KEY,
    sname VARCHAR(20) NOT NULL,
    sgender CHAR(2),
    sbirth DATE,
    sdept VARCHAR(30)
);

CREATE TABLE Course (
    cno CHAR(8) PRIMARY KEY,
    cname VARCHAR(50) NOT NULL,
    ccredit DECIMAL(3,1),
    cdept VARCHAR(30),
    cteacher VARCHAR(20)
);

CREATE TABLE SC (
    sno CHAR(10),
    cno CHAR(8),
    grade DECIMAL(5,2),
    PRIMARY KEY (sno, cno),
    FOREIGN KEY (sno) REFERENCES Student(sno),
    FOREIGN KEY (cno) REFERENCES Course(cno)
);

-- 查询选修了"数据库原理"且成绩超过 90 分的学生
SELECT s.sname, s.sdept, sc.grade
FROM Student s
JOIN SC sc ON s.sno = sc.sno
JOIN Course c ON sc.cno = c.cno
WHERE c.cname = '数据库原理'
  AND sc.grade >= 90
ORDER BY sc.grade DESC;

-- 创建视图：各课程选课人数
CREATE VIEW CourseEnrollment AS
SELECT c.cno, c.cname, COUNT(sc.sno) AS stu_count
FROM Course c
LEFT JOIN SC sc ON c.cno = sc.cno
GROUP BY c.cno, c.cname;
```

#### 预设问答
- 什么是事务？ACID 特性分别是什么？
- 什么是数据库范式？为什么要规范化？
- 什么是索引？B+ 树索引的优缺点是什么？
- SQL 中 JOIN 的几种类型有什么区别？

---

## 三、软件工程（2门）

### 12. 软件工程

- **难度**: 进阶
- **前置知识**: 程序设计基础
- **主题色**: `#FF6F00`
- **图标**: GitCompare

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 软件工程概述 | 软件危机、软件生命周期、过程模型（瀑布/敏捷/迭代） | 入门 |
| 需求工程 | 需求获取、需求分析、需求规格说明、需求验证 | 进阶 |
| 系统设计 | 模块化设计、高内聚低耦合、设计模式、架构风格 | 进阶 |
| 架构设计 | MVC/MVVM、微服务、分层架构、事件驱动架构 | 进阶 |
| 详细设计 | UML 建模（类图、时序图、用例图、状态图） | 进阶 |
| 软件构造 | 编码规范、代码重构、版本控制（Git） | 入门 |
| 软件测试 | 单元测试、集成测试、系统测试、验收测试 | 进阶 |
| 软件维护 | 维护类型、逆向工程、重构、遗留系统管理 | 进阶 |
| 项目管理 | 估算方法、进度管理（甘特图/燃尽图）、风险管理 | 高级 |
| DevOps | CI/CD、持续交付、基础设施即代码 | 高级 |

#### 预设问答
- 瀑布模型和敏捷开发的核心区别是什么？
- 什么是高内聚低耦合？为什么重要？
- MVC 和 MVVM 架构模式的区别是什么？
- 什么是设计模式？列举常用的设计模式并说明用途。

---

### 13. 软件测试

- **难度**: 进阶
- **前置知识**: 软件工程、程序设计
- **主题色**: `#D32F2F`
- **图标**: Target

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 测试基础 | 测试目标、测试原则、测试级别、测试类型 | 入门 |
| 黑盒测试 | 等价类划分、边界值分析、决策表、正交实验 | 进阶 |
| 白盒测试 | 语句覆盖、分支覆盖、路径覆盖、条件组合覆盖 | 进阶 |
| 单元测试 | 测试框架（JUnit/PyTest）、Mock 对象、TDD | 进阶 |
| 集成测试 | 大爆炸集成、自顶向下、自底向上、持续集成 | 进阶 |
| 系统测试 | 功能测试、性能测试、压力测试、安全测试 | 进阶 |
| 自动化测试 | Selenium、Appium、测试脚本、数据驱动测试 | 高级 |
| 性能测试 | 负载测试、压力测试、JMeter、吞吐量分析 | 高级 |
| 测试管理 | 测试计划、用例管理、缺陷管理、测试报告 | 进阶 |

#### 代码示例（PyTest 单元测试）

```python
import pytest

# 被测函数
def is_valid_phone(phone: str) -> bool:
    """验证手机号格式"""
    if not phone or len(phone) != 11:
        return False
    if not phone.isdigit():
        return False
    if not phone.startswith(('13', '14', '15', '17', '18', '19')):
        return False
    return True

# 等价类划分测试
class TestPhoneValidation:
    def test_valid_phone(self):
        assert is_valid_phone("13812345678") == True
    
    def test_invalid_length(self):
        assert is_valid_phone("1234567890") == False  # 10位
        assert is_valid_phone("123456789012") == False  # 12位
    
    def test_invalid_prefix(self):
        assert is_valid_phone("12345678901") == False  # 非法前缀
    
    def test_empty_string(self):
        assert is_valid_phone("") == False
    
    def test_non_digit(self):
        assert is_valid_phone("138abc56789") == False
    
    @pytest.mark.parametrize("phone", [
        "13800000000", "15912345678", "18800001111"
    ])
    def test_multiple_valid(self, phone):
        assert is_valid_phone(phone) == True
```

#### 预设问答
- 黑盒测试和白盒测试的区别是什么？各自有哪些方法？
- 什么是 TDD（测试驱动开发）？它的流程是什么？
- 单元测试、集成测试和系统测试有什么区别？
- 什么是边界值分析？举例说明。

---

## 四、人工智能方向（8门）

### 14. 离散数学

- **难度**: 进阶
- **前置知识**: 高中数学
- **主题色**: `#8E24AA`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 数理逻辑 | 命题逻辑、谓词逻辑、逻辑等价与推理规则 | 进阶 |
| 集合论 | 集合运算、关系（等价/偏序）、函数映射 | 入门 |
| 图论 | 欧拉图/哈密顿图、树、匹配、网络流、平面图 | 进阶 |
| 代数系统 | 群/环/域、布尔代数、格、同态与同构 | 高级 |
| 组合数学 | 排列组合、生成函数、容斥原理、鸽巢原理 | 进阶 |

#### 预设问答
- 命题逻辑和谓词逻辑的区别是什么？
- 什么是欧拉路径和哈密顿路径？有什么区别？
- 等价关系和偏序关系的定义和区别是什么？
- 生成函数在组合计数中的应用是什么？

---

### 15. 概率论与数理统计

- **难度**: 进阶
- **前置知识**: 高等数学
- **主题色**: `#00897B`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 概率基础 | 样本空间、事件概率、条件概率、贝叶斯公式 | 入门 |
| 随机变量 | 离散/连续型分布、分布函数、期望、方差 | 进阶 |
| 多维随机变量 | 联合分布、边缘分布、协方差、相关系数 | 进阶 |
| 大数定律与中心极限定理 | 切比雪夫不等式、LLN、CLT | 高级 |
| 参数估计 | 点估计（矩估计/MLE）、区间估计 | 进阶 |
| 假设检验 | 显著性水平、t 检验、卡方检验、p 值 | 进阶 |
| 回归分析 | 线性回归、多元回归、逻辑回归 | 进阶 |

#### 预设问答
- 条件概率和贝叶斯公式的核心思想是什么？
- 大数定律和中心极限定理各自描述了什么？
- 什么是最大似然估计（MLE）？它的基本原理是什么？
- 第一类错误和第二类错误的区别是什么？

---

### 16. 人工智能导论

- **难度**: 入门
- **前置知识**: 无
- **主题色**: `#00BCD4`
- **图标**: Brain

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| AI 概述 | AI 定义与目标、图灵测试、AI 发展简史 | 入门 |
| 问题求解 | 状态空间搜索、盲目搜索（BFS/DFS）、启发式搜索（A*） | 进阶 |
| 知识与推理 | 谓词逻辑表示、产生式规则、框架表示、语义网络 | 进阶 |
| 机器学习入门 | 监督/无监督/强化学习概述、模型评估基本概念 | 入门 |
| 神经网络基础 | 感知机、前馈网络、激活函数、反向传播概论 | 进阶 |
| 自然语言处理入门 | NLP 基本任务、词向量概念、语言模型 | 入门 |
| 计算机视觉入门 | 图像表示、边缘检测、特征提取 | 入门 |
| AI 伦理 | AI 安全、偏见与公平、可解释性 | 入门 |

#### 代码示例（A* 搜索算法）

```python
import heapq

def a_star_search(start, goal, heuristic, neighbors):
    """A* 搜索算法"""
    open_set = [(0 + heuristic(start, goal), 0, start, [start])]
    closed_set = set()
    
    while open_set:
        f, g, current, path = heapq.heappop(open_set)
        
        if current == goal:
            return path, g
        
        if current in closed_set:
            continue
        closed_set.add(current)
        
        for neighbor, cost in neighbors(current):
            if neighbor not in closed_set:
                new_g = g + cost
                new_f = new_g + heuristic(neighbor, goal)
                heapq.heappush(open_set, (new_f, new_g, neighbor, path + [neighbor]))
    
    return None, float('inf')

# 示例：网格地图最短路径
def heuristic(a, b):
    return abs(a[0] - b[0]) + abs(a[1] - b[1])

def neighbors(pos):
    dirs = [(0,1),(1,0),(0,-1),(-1,0)]
    return [( (pos[0]+d[0], pos[1]+d[1]), 1 ) for d in dirs]

path, cost = a_star_search((0,0), (3,3), heuristic, neighbors)
print(f"最短路径: {path}, 总代价: {cost}")
```

#### 预设问答
- 什么是图灵测试？它有什么意义和局限性？
- 启发式搜索中，启发函数对搜索效率有什么影响？
- 人工智能、机器学习和深度学习之间的关系是什么？
- 什么是 AI 对齐问题？

---

### 17. 机器学习

- **课程 ID**: `ai-ml`
- **难度**: 进阶
- **前置知识**: Python 基础、概率统计基础
- **主题色**: `#00d4ff`
- **图标**: Brain

> **详细内容已在项目 `CourseData.ts` 中完整实现，以下为概要，完整数据请参阅代码。**

#### 核心知识点

**机器学习概述（ml-intro）：**
- 机器学习定义、学习范式分类（监督/无监督/半监督/强化学习）
- 机器学习流程：数据采集 → 清洗 → 特征工程 → 训练 → 评估 → 部署

**监督学习算法（ml-supervised）：**
- K-近邻算法（KNN）、决策树、支持向量机（SVM）
- 随机森林、XGBoost

**无监督学习（ml-unsupervised）：**
- K-Means 聚类、DBSCAN 聚类
- 主成分分析（PCA）

**标准 ML 流程：**

| 阶段 | 描述 | 可调参数 |
|------|------|---------|
| 数据采集 | 收集原始数据 | — |
| 数据清洗 | 处理缺失值、异常值 | 缺失值策略、异常值阈值 |
| 特征工程 | 特征编码、标准化、降维 | 标准化方法、降维方法 |
| 数据集划分 | 训练/验证/测试集 | 训练集/验证集比例 |
| 模型训练 | 算法选择与超参数调优 | 算法选择、交叉验证折数 |
| 模型评估 | 测试集评估与误差分析 | — |

**代码示例：** KNN 从零实现（鸢尾花数据集准确率 0.9667）、PyTorch 简单神经网络、决策树分类

---

### 18. 深度学习

- **课程 ID**: `ai-dl`
- **难度**: 进阶
- **前置知识**: 机器学习基础、线性代数
- **主题色**: `#7c3aed`
- **图标**: Layers

> **详细内容已在项目 `CourseData.ts` 中完整实现，以下为概要。**

#### 核心知识点

**神经网络基础（dl-basics）：**
- 感知机、多层感知机（MLP）
- 反向传播、激活函数（ReLU、Sigmoid、Tanh、Softmax）

**卷积神经网络（dl-cnn）：**
- 卷积运算、池化层（最大池化/平均池化）
- 经典架构：LeNet、AlexNet、VGG、ResNet

**代码示例：** NumPy 实现单层神经网络

---

### 19. 自然语言处理

- **课程 ID**: `ai-nlp`
- **难度**: 进阶
- **前置知识**: 深度学习、Python 编程
- **主题色**: `#06d6a0`
- **图标**: FileText

> **详细内容已在项目 `CourseData.ts` 中完整实现，以下为概要。**

#### 核心知识点

**NLP 基础（nlp-basics）：**
- 分词技术（规则/统计/神经网络的文本分词）
- 词向量（Word2Vec、GloVe、FastText）
- 序列标注（POS Tagging、NER、Chunking）

**Transformer 与预训练模型（nlp-transformer）：**
- 注意力机制（Query-Key-Value 注意力计算）
- BERT（双向编码器，MLM+NSP 预训练）
- GPT 系列（自回归语言模型，Scaling Law）

**NLP 文本处理流程：** 文本预处理 → 词向量表示 → 序列建模 → 输出生成

---

### 20. 计算机视觉

- **课程 ID**: `ai-cv`
- **难度**: 进阶
- **前置知识**: 深度学习、线性代数
- **主题色**: `#f59e0b`
- **图标**: Eye

#### 核心知识点

**图像分类（cv-classification）：**
- 经典分类网络：AlexNet、VGG、ResNet、EfficientNet
- 迁移学习：利用预训练模型微调

**目标检测（cv-detection）：**
- Faster R-CNN（两阶段检测：RPN + ROI Head）
- YOLO 系列（单阶段实时检测算法）

---

### 21. 强化学习 / 生成式AI

- **课程 ID**: `ai-rl`
- **难度**: 高级
- **前置知识**: 机器学习基础、概率论
- **主题色**: `#f43f5e`
- **图标**: Zap

#### 核心知识点

**强化学习基础（rl-basics）：**
- 马尔可夫决策过程（状态、动作、奖励、转移概率）
- 贝尔曼方程（价值函数与最优策略的递归关系）

**深度强化学习（rl-deep）：**
- Deep Q-Network（用神经网络近似 Q 函数）
- PPO（近端策略优化）

**生成式AI扩展：**
- 生成对抗网络（GAN）：生成器与判别器对抗训练
- 扩散模型：前向加噪与反向去噪
- 变分自编码器（VAE）：隐变量生成模型
- Transformer 解码器：自回归文本/图像/视频生成
- 多模态生成：文本到图像（Stable Diffusion）、文本到视频

#### 代码示例（DQN 核心逻辑）

```python
import numpy as np

class SimpleDQN:
    def __init__(self, state_dim, action_dim, lr=0.001, gamma=0.99):
        self.state_dim = state_dim
        self.action_dim = action_dim
        self.gamma = gamma
        # 简单线性 Q 网络
        self.weights = np.random.randn(state_dim, action_dim) * 0.01
    
    def predict(self, state):
        """Q 值预测"""
        return np.dot(state, self.weights)
    
    def update(self, state, action, reward, next_state, done):
        """Q-learning 更新"""
        q_values = self.predict(state)
        q_next = self.predict(next_state)
        target = reward + (0 if done else self.gamma * np.max(q_next))
        q_values[action] += 0.001 * (target - q_values[action])
        self.weights[action] += 0.001 * (target - q_values[action]) * state
    
    def act(self, state, epsilon=0.1):
        """ε-贪心策略"""
        if np.random.random() < epsilon:
            return np.random.randint(self.action_dim)
        return np.argmax(self.predict(state))
```

---

## 五、前沿与应用（3门）

### 22. 计算机图形学

- **难度**: 高级
- **前置知识**: 线性代数、C++程序设计
- **主题色**: `#2E7D32`
- **图标**: Eye

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 图形系统概述 | 图形流水线、光栅化、GPU 架构 | 入门 |
| 二维图形 | 坐标变换（平移/旋转/缩放）、裁剪算法 | 进阶 |
| 三维图形 | 三维变换、投影（透视/正交）、视口变换 | 进阶 |
| 光照与着色 | Phong 光照模型、Blinn-Phong、着色器编程 | 高级 |
| 曲线与曲面 | Bézier 曲线、B-样条、NURBS | 高级 |
| 纹理映射 | UV 映射、Mipmap、法线贴图 | 进阶 |
| 光线追踪 | 光线求交、路径追踪、全局光照 | 高级 |
| 计算机动画 | 关键帧插值、骨骼动画、运动捕捉 | 高级 |

#### 预设问答
- 图形渲染流水线的主要阶段有哪些？
- Phong 光照模型由哪几部分组成？
- 光线追踪和光栅化渲染的区别是什么？
- 什么是纹理映射？Mipmap 解决了什么问题？

---

### 23. 信息安全基础

- **难度**: 进阶
- **前置知识**: 计算机网络、操作系统
- **主题色**: `#C62828`
- **图标**: Shield

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 信息安全概述 | CIA 三要素、安全威胁分类、安全机制 | 入门 |
| 密码学基础 | 对称加密（AES/DES）、非对称加密（RSA/ECC） | 进阶 |
| 哈希与数字签名 | MD5/SHA 系列、HMAC、数字签名算法 | 进阶 |
| 身份认证 | 口令认证、多因素认证、生物特征认证 | 进阶 |
| 访问控制 | DAC/MAC/RBAC、权限管理、安全审计 | 进阶 |
| 网络安全攻防 | DoS/DDoS、SQL 注入、XSS、CSRF、中间人攻击 | 高级 |
| Web 安全 | HTTPS/TLS、OWASP Top 10、安全编码规范 | 进阶 |
| 系统安全 | 缓冲区溢出、提权、rootkit、安全加固 | 高级 |
| 隐私保护 | 数据脱敏、差分隐私、同态加密 | 高级 |

#### 预设问答
- 对称加密和非对称加密的区别是什么？各自的应用场景？
- 什么是 OWASP Top 10？列举几个常见 Web 安全漏洞。
- 什么是 XSS 和 CSRF 攻击？如何防御？
- HTTPS 的握手过程是怎样的？

---

### 24. 大数据与云计算

- **难度**: 高级
- **前置知识**: 数据库、操作系统、计算机网络
- **主题色**: `#00695C`
- **图标**: Layers

#### 核心知识点

| 概念 | 描述 | 难度 |
|------|------|------|
| 大数据概述 | 大数据的 5V 特征、Hadoop 生态体系 | 入门 |
| 分布式文件系统 | HDFS 架构、数据副本策略、读写流程 | 进阶 |
| 分布式计算框架 | MapReduce 编程模型、Spark RDD/Dataset/DataFrame | 进阶 |
| 数据仓库与 ETL | Hive、数据分层、数据清洗与转换 | 进阶 |
| 流式计算 | Kafka、Flink、Storm 实时流处理 | 高级 |
| NoSQL 数据库 | HBase、MongoDB、Cassandra、Redis | 进阶 |
| 云计算基础 | IaaS/PaaS/SaaS、虚拟化技术、容器化 | 入门 |
| 云服务模型 | AWS/GCP/Azure 核心服务、Serverless | 进阶 |
| 容器与编排 | Docker、Kubernetes 核心概念与架构 | 进阶 |
| 云原生技术 | 微服务、服务网格（Istio）、声明式 API | 高级 |

#### 代码示例（MapReduce WordCount）

```python
# MapReduce WordCount 示例
# Map 阶段
def map_function(document):
    """将文档拆分为单词，每个单词出现一次"""
    words = document.lower().split()
    for word in words:
        # 去除标点
        word = word.strip('.,!?;:"\'()[]{}')
        if word:
            yield (word, 1)

# Reduce 阶段
def reduce_function(word, counts):
    """对同一个单词的所有计数求和"""
    total = sum(counts)
    return (word, total)

# 模拟 MapReduce 执行
documents = [
    "Hello world hello",
    "MapReduce is powerful",
    "World of big data",
]

# Map 阶段
mapped = []
for doc in documents:
    for kv in map_function(doc):
        mapped.append(kv)

# Shuffle 阶段（按 key 分组）
shuffled = {}
for word, count in mapped:
    if word not in shuffled:
        shuffled[word] = []
    shuffled[word].append(count)

# Reduce 阶段
result = [reduce_function(word, counts) for word, counts in shuffled.items()]
for word, count in sorted(result, key=lambda x: -x[1]):
    print(f"{word}: {count}")
```

#### 预设问答
- Hadoop 的 HDFS 和 MapReduce 分别是做什么的？
- Spark 相比 MapReduce 有哪些优势？
- IaaS、PaaS、SaaS 三种云服务模式的区别是什么？
- Docker 和虚拟机有什么区别？Kubernetes 解决了什么问题？

---

## 学习场景与辅导模式

系统定义了 **4 大学习场景**，每个场景含多个子模式：

### 课前预习
- **概念速览**: 快速了解核心概念
- **案例式科普**: 通过案例理解抽象内容

### 课中解惑
- **碎片答疑**: 精简回答不打断节奏
- **跟不上了**: 重新讲解当前知识点

### 课后实验
- **代码辅导**: 逐行指导代码编写
- **Bug 调试**: 定位并修复代码错误
- **报告撰写**: 规范实验报告格式

### 考前冲刺
- **考点梳理**: 梳理高频考点
- **题型解析**: 典型题详解
- **大作业指导**: 项目全流程指导

---

## 代码示例汇总

| 序号 | 代码示例 | 所属课程 | 语言 |
|------|---------|---------|------|
| 1 | Fibonacci 数列递归实现 | C 语言 | C |
| 2 | 指针交换函数 | C 语言 | C |
| 3 | Python 装饰器与快速排序 | Python | Python |
| 4 | Python 生成器与斐波那契 | Python | Python |
| 5 | Java 泛型类 + Stream + 多线程 | Java OOP | Java |
| 6 | C++ 模板 + 智能指针 + STL | C++ OOP | C++ |
| 7 | C++ RAII 文件处理器 | C++ OOP | C++ |
| 8 | KMP 字符串匹配算法 | 数据结构 | Python |
| 9 | 0-1 背包动态规划 | 算法设计 | Python |
| 10 | A\* 搜索算法 | 人工智能导论 | Python |
| 11 | 递归下降语法分析计算器 | 编译原理 | Python |
| 12 | TCP 套接字回显服务器 | 计算机网络 | Python |
| 13 | 生产者消费者（信号量实现） | 操作系统 | Python |
| 14 | 手机号验证 PyTest 测试 | 软件测试 | Python |
| 15 | 学生选课系统 SQL | 数据库原理 | SQL |
| 16 | MapReduce WordCount | 大数据与云计算 | Python |
| 17 | DQN 核心逻辑 | 强化学习 | Python |
| 18 | 数据缺失值处理（Pandas） | 机器学习 | Python |
| 19 | KNN 分类器从零实现 | 机器学习 | Python |
| 20 | PyTorch 神经网络 | 机器学习 | Python |
| 21 | NumPy 实现单层神经网络 | 深度学习 | Python |
| 22 | Transformer 注意力机制 | 自然语言处理 | Python |
| 23 | FastAPI 模型服务部署 | 机器学习工程 | Python |

---

## 学习资源

系统内置分类资源，按课程方向整理：

| 方向 | 资源数 | 典型资源 |
|------|--------|---------|
| 机器学习 | 7 | Python 机器学习入门指南、监督学习算法全景图、ML 面试 100 题 |
| 深度学习 | 4 | 深度学习知识图谱、反向传播完整推导、PyTorch 模板 |
| 自然语言处理 | 4 | Transformer 原理解析、BERT 文本分类实战、NLP 全景图 |
| 大模型与 LLM | 5 | Prompt Engineering 完全指南、RAG 系统实现、AI Agent 实战 |
| 计算机视觉 | 3 | 目标检测综述、YOLOv8 教程、图像分类迁移学习 |
| 强化学习 | 2 | RL 从 MDP 到 PPO、DQN 玩转 Atari |
| MLOps | 3 | MLflow 实战、FastAPI 模型部署、模型监控方案 |

---

## 学习路径

系统推荐的 **20 周学习路线**：

| 阶段 | 周次 | 内容 | 状态 |
|------|------|------|------|
| 基础夯实 | 第 1-4 周 | Python 科学计算、概率论与数理统计、线性代数 | 已完成 |
| 机器学习核心 | 第 5-10 周 | ML 概论、KNN/决策树、SVM/集成学习、聚类降维、模型评估 | 进行中 |
| 深度学习专题 | 第 11-16 周 | 神经网络、CNN 图像识别、Transformer、NLP 预训练模型 | 进行中 |
| 实战与拓展 | 第 17-20 周 | LLM 应用开发、AI Agent 项目、模型部署与服务 | 已锁定 |

**里程碑成就：** Python 启航 → 算法入门 → 机器学习 → 深度学习 → 大模型实战 → 全栈 AI 工程师

---

> 此文档基于项目 `src/components/course/CourseData.ts`、`src/types/course.ts`、`src/views/` 等源文件提取，并按照"编程与算法基础 / 计算机系统 / 软件工程 / 人工智能方向 / 前沿与应用"五大方向重新组织生成。
